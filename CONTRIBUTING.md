# 协作规范

- `main` 分支保持可运行。
- 每项任务使用短期功能分支，不建立长期 A、B、C 个人分支。
- 每个 PR 只处理一个明确任务。
- 代码、接口、Mock 和对应文档在同一个 PR 更新。
- 模块主责人提出修改，受影响人员负责确认。
- 跨模块修改至少由一名使用方确认。
- 不提交构建产物、密钥、个人配置和临时文件。
- 文档使用中文，代码标识符使用英文。
- Day 4、Day 8 和最终交付建立可回退版本标签。

仓库使用 Git LFS 管理模型、音频、PPT 和视频等大型二进制文件。首次参与前安装 Git LFS，并执行 `git lfs install`。
